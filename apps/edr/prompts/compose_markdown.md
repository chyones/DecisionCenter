# Compose Markdown Prompt

Convert the validated JSON report into the executive Markdown template.

Rules:

- Preserve the approved claims and evidence IDs.
- Do not add new facts.
- Include the missing-data section even when empty.

# UAT_RUN — 2026-06-12

Phase 2D Slice 6 (Real UAT Flow) evidence. This is the first `UAT_RUN_<date>`
artifact; it records what was exercised against the **real** deployed
runtime and integrations this session, with no mocked backend. Business
content is summarised, not reproduced. System status: **NOT_LIVE**.

## Environment

- App container rebuilt on commit `4ef1e8a` (process start 2026-06-12 14:03);
  `/healthz` → 200; `LLM_PROVIDER=deepseek`; deployed `llm.py` carries the
  DeepSeek v4 model fix and JSON fence-strip fix.
- DeepSeek key present (live, balance topped up). Langfuse **not configured**
  (keys empty) — tracing N/A this run.

## Steps exercised against real services

| # | Step | Real backend | Result |
|---|---|---|---|
| 1 | Generation provider live | api.deepseek.com | **PASS** — `tier="standard"`→`deepseek-v4-flash`, `heavy`→`deepseek-v4-pro`, HTTP 200, usage tokens, no fallback (see `DEEPSEEK_MODEL_NAME_FIX_2026-06-12.md`) |
| 2 | Full 18-node report graph | in-process `run_workflow` on deployed commit | **PASS** — all nodes; 5/5 DeepSeek HTTP 200; tokens 1281/2220; cost USD 0.0042; no fallback; report JSON produced |
| 3 | Odoo evidence | app→n8n→erp.elrace.com | **PASS** — `odoo-read` HTTP 200, real `project.project` evidence (id 14435) |
| 4 | SharePoint Graph retrieval | Microsoft Graph (app token) | **PASS at source** — `search(q='…')` HTTP 200, 200 items; **BLOCKED in workflow** by the n8n credential defect (fix staged, see §B of `GO_LIVE_BLOCKER_RESOLUTION_2026-06-12.md`) |
| 5 | n8n health + webhook auth | n8n container | **PASS** — `/healthz` 200; bearer webhook auth enforced (403 without, 200 with) |
| 6 | Entra validator vs v2 tokens | tenant OIDC/JWKS | **PASS (config+code)** — manifest `requestedAccessTokenVersion=2`, validator accepts v2; live user-token smoke is operator-gated |
| 7 | Quality gate / approval / publish | graph nodes | Quality gate ran and returned `failed` honestly (retrieval evidence empty in-process — `n8n:5678` not resolvable from runner) |

## Not exercised (operator-gated, documented — not failures)

- **Deployed `POST /reports/staging` via the real auth surface**: needs an
  interactive Entra **user** token (cannot be minted head-lessly; the API's
  app roles are User-only by design).
- **SharePoint/Mail evidence flowing into a live report**: needs the staged
  n8n credential fix imported into the running n8n (operator: n8n CLI/editor).
- **Mail evidence**: additionally blocked by the group-mailbox endpoint issue
  (design decision pending).
- **Langfuse trace**: not configured.

## Outcome

Generation, Odoo retrieval, n8n transport, and the full graph are proven
against real services; the remaining gaps to a clean single end-to-end run
are the two operator-gated items above. This satisfies Slice 6's "real
integrated flow, no mocked backend" intent for the components reachable
autonomously and pins the exact operator actions for the rest. Production
remains **NOT_LIVE**.

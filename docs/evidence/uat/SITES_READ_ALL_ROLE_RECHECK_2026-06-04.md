# Sites.Read.All Role Recheck Evidence

**Date (evidence run):** 2026-06-05T04:35:51Z
**Gate context:** Gate 3 pre-condition recheck (Sites.Read.All was missing in prior run)
**Production state:** NOT_LIVE

---

## Background

Gate 3 (SharePoint mapping discovery) was blocked because `Sites.Read.All` was absent
from the Microsoft Graph client-credentials token issued to API app `a2160d26`.

Operator action before this recheck:
- Verified Azure portal: `Sites.Read.All` added as Microsoft Graph **Application** permission on app `a2160d26`.
- Admin consent granted.
- App restarted to flush token cache.

---

## Token Acquisition

| Field | Value |
|---|---|
| Tenant ID | `14a72467-3f25-4572-a535-3d5eddb00cc5` |
| Client (App) ID | `a2160d26-acc0-4d8c-b815-3a377f1fb5bd` |
| Client Secret | `[REDACTED]` |
| Token endpoint | `https://login.microsoftonline.com/{tenant}/oauth2/v2.0/token` |
| Scope | `https://graph.microsoft.com/.default` |
| Token type | `Bearer` |
| Token TTL | `3599 s` |
| Token length | `2083 chars` |

Token was acquired fresh at recheck time. No cached token used.

---

## Role Verification (JWT payload `.roles` claim)

| Permission | Required | Result |
|---|---|---|
| `Files.Read.All` | YES | **PRESENT** |
| `Mail.Read` | YES | **PRESENT** |
| `Sites.Read.All` | YES | **PRESENT** |

All three Gate 3 pre-condition roles are confirmed in the token.

---

## Prior Blocker Analysis (resolved)

The prior absence of `Sites.Read.All` was consistent with one of four root causes:

| Cause | Status |
|---|---|
| Wrong app targeted in Azure portal | Ruled out — same `a2160d26` confirmed |
| Permission added as **Delegated** instead of **Application** | Resolved — operator confirmed Application type |
| Admin consent not granted | Resolved — operator confirmed consent granted |
| Token cache not flushed after grant | Resolved — app restarted before recheck |

---

## Verdict

```
SITES_READ_ALL_ROLE_CONFIRMED_NOT_LIVE
```

Gate 3 mapping discovery may proceed when the operator is ready.
Production remains NOT_LIVE pending full UAT sign-off.

---

*Evidence generated by Claude Code recheck run — 2026-06-05T04:35:51Z*

# UAT Run — 2026-06-15 (PRJ-001 evidence + generation)

**Status: NOT_LIVE (unchanged).** Records a clean evidence + generation run after the n8n
SharePoint webhook routing repair (`N8N_WEBHOOK_REPAIR_2026-06-15.md`) and the Odoo evidence
fix (commit `4d0e861`). No secrets printed. LIVE not marked.

## Scope of this run

This is the full 18-node report graph (`run_workflow`, the exact function `POST /reports/staging`
calls) for **PRJ-001**, exercised against the **live n8n webhooks** with a real Microsoft Graph
client-credentials token. It proves SharePoint + Odoo evidence now flow into the report and that
generation is DeepSeek-served with no fallback.

It is **not** the final deployed-API UAT: the deployed `/reports/staging` call through the
public auth surface needs an interactive **Entra user** token (cannot be minted head-lessly),
and approve→publish→download is gated on that. Those remain operator steps (see Remaining).

## Result

| Check | Result |
|---|---|
| Workflow nodes visited | 18 / 18 (`node_00_begin` … `node_17_publish`) |
| SharePoint webhook (direct probe, real Graph token) | HTTP 200, **200 items**, non-empty `{"evidence":[...]}` |
| SharePoint evidence in report graph | **89 items** (`sharepoint_status: ok (89 items)`) |
| Odoo evidence in report graph | **1 item** (`odoo_status: ok (1 items)`) — `odoo-project-project-14602`, "Construction of Civil Defense building in Al Marfa" |
| Evidence by source | `{sharepoint: 89, odoo: 1}` |
| DeepSeek calls | 4, all HTTP 200 |
| DeepSeek fallback used | **no** (`FALLBACK_DETERMINISTIC` absent) |
| Cost (this run) | ~$0.030 |
| Quality gate | **passed** |
| publish_status | `blocked_until_approval` (host runner cannot resolve `postgres`; deployed API publishes normally; gracefully caught) |

## What this confirms

- The Odoo zero-evidence defect (commit `4d0e861`) is resolved live: the report graph now
  retrieves the project record via the real `id` column.
- The SharePoint webhook HTTP 500 is resolved (n8n webhook repair, 2026-06-15): SharePoint
  evidence reaches the report.
- Generation is DeepSeek-served end-to-end with no deterministic fallback.

## Remaining before go-live (operator)

1. Deployed `/reports/staging` call through `https://vantage.elrace.com` with an interactive
   Entra **user** token (`scripts/validate_entra_auth.py --base-url https://vantage.elrace.com
   --token "<user token>"`), then a full approve → publish → download cycle.
2. Sign the approval block in `SLICE7_GO_LIVE_READINESS_2026-06-15.md`.

System remains **NOT_LIVE** until the deployed-API cycle is captured and the approval is signed.

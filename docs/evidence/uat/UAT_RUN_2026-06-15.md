# UAT Run — 2026-06-15 (PRJ-001 multi-source evidence + generation)

**Status: NOT_LIVE (unchanged).** Records a clean **multi-source** evidence + generation run
after: the Odoo evidence fix (`4d0e861`), the n8n SharePoint webhook repair
(`N8N_WEBHOOK_REPAIR_2026-06-15.md`), the mandatory multi-source coverage work
(`MULTI_SOURCE_PROJECT_REPORT_VALIDATION_2026-06-15.md`), and the post-deployment
live verification after commit `3ff35e3`.  No secrets printed. LIVE not marked.

---

## Run 1 — In-process graph (pre-deployment, commit f63c311)

Full 18-node report graph (`run_workflow`) for **PRJ-001** exercised against live
connectors with a real Graph client-credentials token and live DeepSeek.
Query: **"give me small sumary for this project"**, role executive.

| Check | Result |
|---|---|
| Workflow nodes visited | 18 / 18 |
| Odoo — attempted / evidence / status | yes / **101** / ok (1 project + 100 real `account.analytic.line` cost lines) |
| SharePoint — attempted / evidence / status | yes / **199** / ok (keyword fallback used the project name) |
| Email — attempted / evidence / status | yes / **2** / ok (`/groups/{id}/conversations`, path `group_conversations`) |
| Evidence by source | `{sharepoint: 199, email: 2, odoo: 101}` |
| Connector coverage in report | present (per-source enabled/attempted/count/status/reason) |
| Evidence completeness | **full** |
| DeepSeek calls | 4, all HTTP 200 |
| DeepSeek fallback used | **no** |
| Odoo financial | real cost evidence present (`odoo_financial_available=true`); no invented budget/actual_cost |
| Quality gate | **passed** |
| publish_status | `blocked_until_approval` (host runner cannot resolve `postgres`; deployed API publishes normally; gracefully caught) |

---

## Run 2 — Post-deployment live verification (commit 3ff35e3, 2026-06-15)

App rebuilt from commit `3ff35e3`, n8n webhook auth re-bound (bind script confirmed
`sharepoint_search` + `email_search` + `odoo_read` all bound), n8n restarted.
All verification executed from inside the `decisioncenter-app-1` container so that
Docker-network hostnames (`n8n`, `postgres`, `minio`) resolve correctly.

### Infrastructure health

| Service | Status |
|---|---|
| App (`/healthz`) | `status: ok` — postgres/redis/qdrant/minio all `ok`, 18 workflow nodes |
| n8n (`/healthz` via Docker network) | `status: ok` |
| Postgres | `healthy` (Docker health) |
| Redis | `healthy` (Docker health) |
| MinIO | `healthy` (Docker health) |
| Qdrant | running |
| Caddy / cloudflared | running (HTTPS edge up) |

### Connector direct probes (from inside app container)

| Connector | Path | HTTP | Count | Detail |
|---|---|---|---|---|
| SharePoint | n8n `/webhook/sharepoint-search` (POST) | 200 | **199** | keyword=project name; first id `sp-01XMH44T…` |
| Odoo project | n8n `/webhook/odoo-read` (POST) | 200 | **1** | `project.project` id=14602 |
| Odoo cost | n8n `/webhook/odoo-read` (POST) | 200 | **100** | `account.analytic.line`, analytic_account=21963; first: "Gross Staff / −6500 / 2026-05-31" |
| Email | Graph `/groups/71313630…/conversations` (direct) | 200 | **2** | group_conversations path; real conversation subjects |
| Entra app token | Graph `/organization` | 200 | tenant: EL RACE CONS & GEN CONT CO LLC | v2 token confirmed |

### Full 18-node graph run (inside container, live connectors)

Query: **"give me small sumary for this project"**, role executive, PRJ-001.
DeepSeek provider: `deepseek`, key available, fallback path not taken.

| Check | Result |
|---|---|
| Committed code | `3ff35e3` |
| Elapsed | 83 s |
| Nodes visited | 18 / 18 |
| Odoo evidence | **101** (1 project + 100 cost lines) / status=ok / `odoo_financial_available=True` |
| SharePoint evidence | **199** / status=ok / term used: project name (keyword fallback) |
| Email evidence | **2** / status=ok / path=group_conversations |
| Total evidence | **302** |
| Evidence completeness | **full** |
| DeepSeek provider | deepseek (key_available=True, fallback=no) |
| Quality gate | **passed** |
| `connector_coverage` in report JSON | **yes** |
| `quality_gate_status` in report JSON | **yes** |
| `evidence_completeness` in report JSON | **yes** |
| publish_status | `blocked_until_approval` (approval workflow gate; expected) |

### Connector coverage (from report JSON)

| Source | Enabled | Attempted | Count | Status |
|---|---|---|---|---|
| odoo | yes | yes | 101 | ok |
| sharepoint | yes | yes | 199 | ok |
| email | yes | yes | 2 | ok |

---

## Clean-UAT checklist (requirement 7)

- [x] Odoo attempted — `count=101`, `status=ok`
- [x] SharePoint attempted — `count=199`, `status=ok`, keyword fallback active
- [x] Email attempted — `count=2`, `status=ok`, group-conversations path
- [x] Evidence counts visible in connector coverage section
- [x] DeepSeek fallback = **no** (provider=deepseek, key_available=True)
- [x] Sources listed match connector coverage (all three: odoo/sharepoint/email)
- [x] Entra app token live (GET /organization → 200, correct tenant)
- [x] All n8n webhooks bound and returning data post-restart
- [x] App healthz = ok (all infra services ok, 18 nodes)

**Graph-level UAT is clean.** The deployed-API + Entra-user-token + approve/publish/download
cycle is the remaining go-live step.

---

## Remaining before go-live (operator)

1. Deployed `/reports/staging` via `https://vantage.elrace.com` with an interactive Entra
   **user** token (`scripts/validate_entra_auth.py --base-url https://vantage.elrace.com --token "<user token>"`),
   then a full approve → publish → download cycle.
2. Sign the approval block in `SLICE7_GO_LIVE_READINESS_2026-06-15.md`.

System remains **NOT_LIVE** until the deployed-API cycle is captured and the approval is signed.
